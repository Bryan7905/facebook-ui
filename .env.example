# Example environment variables for Facebook-ui (Vite)
# Set VITE_API_BASE to your API base URL (no trailing slash). This is used at build time.
# For local development you can set it to your local backend, e.g. http://localhost:8080
# For production on Render set it to your Render service URL, e.g. https://your-api.onrender.com

VITE_API_BASE=https://facebookapi-8y44.onrender.com
